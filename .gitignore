.DS_Store
__pycache__/
.claude/settings.local.json
# nix build / npm 実行時の残骸
result
.npm/
node_modules/
# codegraph のローカルインデックス（codegraph init で再生成可能）
.codegraph/
# relink フック (relink-claude-files.sh) が退避する旧内容バックアップ
*.relinkbak.*
